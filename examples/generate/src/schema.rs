#[derive(Debug, Clone)]
pub struct Users {
    pub id: i32,
    pub name: String,
    pub name2: String,
}
#[derive(Debug, Clone)]
pub struct Orders {
    pub id: i32,
    pub name: String,
}
#[derive(Debug, Clone)]
pub struct Earnings {
    pub id: i32,
    pub dope: String,
}
#[derive(Debug, Clone)]
pub struct Data {
    pub id: i32,
}
